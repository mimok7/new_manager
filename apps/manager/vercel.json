{
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREV_SHA $VERCEL_GIT_COMMIT_SHA -- apps/manager",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @sht/manager build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
